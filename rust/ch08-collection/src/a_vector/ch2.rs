pub fn exec(_str: &String) {
    let mut v = vec![1, 2, 3];

    let f = &v[0];
    println!("{:?}", f); // ✅

    v.push(4); // move to other memory
    // 컬렉션에서는 확정 길이 메모리를 가지고 있다가,
    // 그 값을 다시 벗어나 값이 추가되면, 연속된 메모리 크기를 다시 할당하기 위해
    // 포인터가 가리키고 있는 메모리 주소가 바뀌게 된다.

    // println!("{:?}", f); // 🚫
    // 여전히 빌림참조는 기존의 메모리를 향하고 있게 되므로,
    // 컬렉션에 - 빌림 -> 추가 -> 빌림사용 은 금지된다.
}